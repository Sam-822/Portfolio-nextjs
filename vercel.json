{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    }
  },
  "commands": {
    "install": "npm install --legacy-peer-deps",
    "build": "next build"
  }
}
